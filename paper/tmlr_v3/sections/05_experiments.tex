\section{Experimental Setup}
\label{sec:experiments}

Table \ref{tab:protocols} separates the evidence used for each claim.  The procurement runs use the
full runtime, the two targeted audits interrogate particular verification boundaries, and PACS and
WTQ evaluate the separate algebra planner.  Their percentages are not points on one learning
curve.

\begin{table}[t]
  \centering
  \caption{Evaluation protocols, available information, and the conclusion each can support.}
  \label{tab:protocols}
  \small
  \begin{tabularx}{\linewidth}{p{0.17\linewidth}p{0.10\linewidth}p{0.29\linewidth}X}
    \toprule
    Protocol & $n$ & Available information & Supported conclusion \\
    \midrule
    Procurement main & 2,025 & Full online runtime without an oracle & Complete system accuracy after development removal \\
    Development ladder & 260 & Same runtime with the oracle used only for scoring & Paired checkpoint changes, not component causality \\
    Grounding replay & 136 & Fixed saved plans with oracle scoring & Effect of the additive value guard on those plans \\
    Repair target audit & 229 & Oracle accepted targets with mapped question constraints & Constraint retention in the auditable subset \\
    PACS & 922 & One algebra tree call without the full runtime & Aggregate accuracy and surface sensitivity for the same checkpoint \\
    WTQ evaluation & 4,344 & One guided algebra tree call with table schema & Portability of typed program generation \\
    \bottomrule
  \end{tabularx}
\end{table}

\paragraph{Procurement main evaluation.}
The frozen final partition contains 2,285 rows.  Its balanced 260 row subset contains 20 questions
from each of 13 buckets and was used for checkpoint selection and pipeline diagnosis.  We therefore
remove those IDs from the primary column, leaving 2,025 questions.  Of these, 1,725 are answerable
and 100 belong to each of the ambiguous, unsupported, and no result classes.  We also report the
inclusive 2,285 row artifact for continuity.  The
eight saved arms are closed-book generation, top-40 RAG, plan-guided RAG, the cloud teacher, hybrid
Qwen and Llama systems with a cloud understander, and fully local Qwen and Llama systems.  All
per item predictions are rescored by the same typed comparator.  Both reported columns derive from
the same saved predictions, with the primary column formed by removing development identifiers and
rescoring the remainder.  The stored run directories do not contain a complete command level
manifest, so neither column claims that every arm can be regenerated from the available artifact
bundle.

\paragraph{Development diagnostic and training ladder.}
On the 260 balanced questions we compare a deterministic decomposition planner (the KG-only
control), RAG, the cloud teacher, and the two local model families.  This is a sequential
checkpoint comparison rather than a matched component ablation because later rungs change both the
training pool and objective.  For Qwen3-8B and Llama-3.1-8B, the planner ladder is untuned, SFT,
rejection sampling fine tuning (RSFT), and DPO.  The SFT adapters use 4-bit QLoRA
on all target modules with rank 64, $\alpha=128$, dropout 0.05, effective batch 16, learning rate
$10^{-4}$, and three epochs.  RSFT continues for two epochs at $5\times10^{-5}$; DPO continues for
one epoch with $\beta=0.1$ and learning rate $5\times10^{-6}$.  Qwen and Llama use the same data
recipe.  During checkpoint evaluation, each learned two-stage arm uses temperature zero, draws two
structural plan samples, and allows at most one feedback replan.  These settings are distinct from
the stochastic RSFT collection budget reported in Section \ref{sec:m-training}.  Since predictions
are paired by question, we report exact McNemar tests.  There
is one training run per checkpoint, so these tests do not measure seed variance.  Appendix
\ref{app:ablations} states which existing comparisons are interventions, checkpoint ladders, or
robustness tests.

\paragraph{Verification boundary audits.}
The runtime intervention replays 136 saved plans with typed oracles before and after hard
grounding.  The plan text and model output remain fixed, so the comparison isolates deterministic
changes made by that grounding path.  In the stored artifact, the only active intervention is the
additive value guard required for sums.  A separate training target audit examines 229 repair
targets for which question constraints can be mapped mechanically.  It compares those mapped
constraints with the exported plan.  This targeted subset can demonstrate omission after runtime
and oracle acceptance, but it cannot estimate omission prevalence over every generated plan.

\paragraph{PACS.}
PACS covers seven capability families and three composition levels.  The current 922-row manifest
has 730 answerable, 138 missing-operator, and 54 empty-result items.  Channel A is generated by an
independent renderer; channel B is a paraphrase that passes literal and logic gates.  Each planner
receives the same algebra contract and makes one temperature-zero, prompt-only JSON call without
repair.  Saved trees are type-checked and executed on the frozen procurement view.

The original channel files treated 36 answerable false or zero-valued rows as no-result.  We correct this
without another model call by joining saved outputs to the current manifest and re-executing their
trees.  Thirty-five channel-A and all 36 channel-B predictions are correct under the repaired
labels.  We use a strict Boolean comparator, so a Boolean never matches integer zero or one.  PACS
excludes exact test questions and tree hashes from Compose-v3 training, but its near-duplicate gate
samples the training trigrams and two rows overlap between Compose-v3 train and validation.  We call
it a compositional diagnostic rather than a fully sealed OOD benchmark.

\paragraph{WikiTableQuestions.}
WTQ uses 4,344 human questions over 421 tables absent from its training-table partition
\citep{pasupat2015compositional}.  The table adapter supplies a dynamic column schema, cell-linking
hints, a typed computation view, and a raw projection view.  Every arm makes one temperature-zero
guided call.  We evaluate the Qwen3-8B base, the procurement Compose-v3 checkpoint, an answer-only
self-harvest adapter (A), and an adapter trained on translated SQUALL gold programs (C)
\citep{shi2020squall}.  Candidate A trees are kept only when execution matches the answer; C trees
are kept only when translation, type checking, execution, and the target agree.  The frozen run
records 4,709 A examples and approximately 5,423 C examples, although the exact C training snapshot
was not preserved in the current Git state.  Accuracy is recomputed from saved answer items with
the official WTQ evaluator v1.0.2 and CoreNLP-tagged targets, including the tab/newline sanitation
used by the frozen runner.
