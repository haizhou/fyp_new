\documentclass{article}

\usepackage[preprint]{tmlr}
% \usepackage{tmlr}            % anonymized submission
% \usepackage[accepted]{tmlr}  % camera-ready

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit}
\graphicspath{{./}{figs/}}

\title{Auditable Compositional Question Answering over UK Public Procurement}

% TODO final author block
\author{\name Anonymous \email anonymous@example.com \\
 \addr University College London}

\def\month{07}
\def\year{2026}
\def\openreview{\url{https://openreview.net/forum?id=XXXX}}

\begin{document}

\maketitle

\begin{abstract}
UK public procurement records are public and partly structured, but reliable
analysis remains difficult. Organisation identifiers are fragmented, monetary
fields have different accounting meanings, and many useful questions require
several joins, filters, and aggregations. We build an auditable question
answering framework from 166{,}277 Open Contracting Data Standard releases.
The framework normalises the source records, represents questions as programs
in a closed typed algebra, and executes each program with deterministic data
conventions. It can also abstain when a question is ambiguous, unsupported, or
not answered by the available records. The resulting benchmark contains
12{,}828 questions with executable gold programs. A second evaluator, written
without shared execution code, reproduces 99.88\% of 14{,}770 audited oracle
cases.

Experiments separate the value of the executable scaffold from the value of
model training. On development data, an untrained 8B model rises from a 31.5\%
retrieval baseline to 70.4\% when routed through the scaffold. Task-specific
training raises the strongest fully local system to 85.65\% on a sealed
2{,}285-question test, compared with 69.76\% for the cloud teacher used to
propose training candidates. The ordering holds with a second 8B base model
and on a sealed procurement challenge set, where the local planner reaches
78.31\% against 50.33\% for the teacher. A transfer to WikiTableQuestions raises an untrained 22.51\%
baseline to 44.43\% with answer-only supervision and 51.80\% with gold
programs. The transfer audit identifies program-language and representation
coverage as the main remaining bottleneck.
\end{abstract}

% -----------------------------------------------------------------------------
\section{Introduction}
\label{sec:intro}

Large language models can interpret natural-language questions and produce
fluent answers, but they remain unreliable when a task requires several linked
operations. A model may retrieve the right records while applying the wrong
relation, counting the wrong unit, or aggregating values that should remain
separate. Structured query systems offer a different strength. Their entities,
relations, and operations can be inspected and executed again. Their weakness
is coverage, since they depend on a defined schema and a restricted query
language. Reliable question answering over structured data therefore requires
both flexible language understanding and a reproducible path from evidence to
answer.

UK public procurement is a demanding setting for this problem. Contract
notices contain buyers, suppliers, awards, dates, locations, and monetary
values, which makes them suitable for factual, temporal, and compositional
analysis. The records are not clean enough to query directly. One organisation
can appear under many source identifiers and name variants. Values may be
reported at tender, lot, award, and contract level, where some fields represent
separate amounts and others repeat the same amount. A program can therefore be
valid and executable while still computing the wrong quantity. Correctness
depends on the program, the data conventions, and the link between the two.

Prior work addresses parts of this setting. Retrieval-augmented generation can
supply relevant records, but it does not guarantee correct joins, counts, or
aggregations. Neural-symbolic question answering maps language to executable
programs, but is usually evaluated over curated schemas and stable execution
environments \citep{cheng2023binder,gu2023pangu,luo2024chatkbqa}. Methods such
as STaR and rejection-sampling fine-tuning learn from selected model outputs
\citep{zelikman2022star,yuan2023scaling,gulcehre2023rest}. These methods show
that executable or answer-based feedback can support training. They do not
resolve the full problem considered here, where the source semantics must first
be audited, refusal is part of the task, and successful execution does not by
itself establish that the program answers the question.

We build the task and system end to end. Five years of procurement releases
are converted into a canonical data layer with explicit rules for entity
identity, value provenance, and monetary additivity. Questions are compiled
into a small typed algebra. A language model proposes the interpretation and
program. Deterministic code then checks the program, grounds it to the data,
and executes it. During training, sampled programs are retained only when their
executed answers match the benchmark oracle. During deployment, no oracle is
available or required. The final answer comes from execution, not free-form
model generation.

The evaluation asks three questions. First, how much of the performance comes
from the executable scaffold, and how much comes from task-specific model
training? Second, does the resulting capability survive new program structures
and question surfaces? Third, when the architecture is moved to another
structured-data domain, which component limits performance? The experiments
show that the scaffold establishes a strong floor, training both question
understanding and planning improves generalisation, and cross-domain transfer
is limited mainly by representation and program-language coverage.

\paragraph{Contributions.}
This work makes three contributions.
\begin{enumerate}
    \item We construct an auditable procurement question-answering task with
    explicit data conventions, executable gold programs, scored abstention,
    and answer oracles checked by an independently implemented evaluator.

    \item We develop a typed executable framework that separates language
    understanding from deterministic computation. The same framework supports
    program training from teacher and self-generated candidates without
    making the language model the source of the final answer.

    \item We provide a controlled empirical decomposition across retrieval,
    executable scaffolding, local planning, sealed external evaluation, and
    table-domain transfer. The analysis identifies where performance comes
    from and where the closed language still limits coverage.
\end{enumerate}

% -----------------------------------------------------------------------------
\section{Related Work}
\label{sec:related}

\paragraph{Executable question answering.}
Semantic parsing and knowledge-base question answering translate natural
language into logical forms that can be executed against structured knowledge.
Recent systems improve coverage through in-context program generation,
graph-constrained search, retrieval-based grounding, or tool use
\citep{cheng2023binder,gu2023pangu,luo2024chatkbqa,luo2024rog,jiang2023structgpt}.
Table question-answering systems use SQL, Python, neural table encoders, or
hybrid execution \citep{pasupat2015compositional,herzig2020tapas,yin2020tabert,
liu2022tapex,jiang2022omnitab,ye2023dater,ni2023lever,wang2024chainoftable}.
These methods establish the value of executable intermediate representations.
Our setting adds a problem that curated benchmarks usually remove. The source
fields and entity identities are themselves uncertain, so execution is only
meaningful after the data conventions have been specified and audited.

\paragraph{Learning from generated programs.}
STaR, rejection-sampling fine-tuning, and ReST generate candidate solutions,
retain those that satisfy an acceptance rule, and train on the survivors
\citep{zelikman2022star,yuan2023scaling,gulcehre2023rest}. V-STaR learns a
verifier from correct and incorrect solutions, while executable SQL systems
use answer or execution feedback to build training data
\citep{vstar2024,zhang2025exesql}. Weak-to-strong work studies when a student
can generalise beyond imperfect supervision \citep{burns2024weak}. We do not
claim the generate-filter-train loop as a new method. We use it as one part of
an executable system and study how it behaves when a program can compile and
run while expressing the wrong analysis.

\paragraph{Procurement data, provenance, and refusal.}
Procurement knowledge graphs such as TheyBuyForYou support data integration,
search, and risk analysis across public records \citep{soylu2022theybuyforyou}.
Our focus is a measurable natural-language task rather than a graph resource
alone. Each answer is tied to an executable program and grounded records.
Questions that are ambiguous, unsupported by the data model, or matched by no
records are represented explicitly, in the spirit of unanswerable-question
benchmarks and selective prediction \citep{rajpurkar2018know,kamath2020selective},
but scored against executable programs rather than span annotations. This connects provenance to the execution
trace and makes failure detection part of the benchmark rather than an
informal property of the interface.

% -----------------------------------------------------------------------------
\section{Task and Method}
\label{sec:method}

\subsection{Overview}

Figure~\ref{fig:architecture} separates deployment from data construction. At
deployment time, the model reads a question and emits a program under
constrained decoding. Deterministic components check, ground, and execute the
program, then return an answer or abstention. During training-data collection,
candidate programs are sampled at positive temperature and compared with the
benchmark oracle after execution. Only matching candidates enter the training
pool. Oracle access is therefore a property of the data pipeline, not the
runtime system.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  font=\small,
  box/.style={draw, rounded corners=2pt, align=center, inner sep=4.5pt, minimum height=2em},
  det/.style={box, fill=blue!6},
  lm/.style={box, fill=orange!12},
  data/.style={box, fill=gray!10},
  arr/.style={-{Stealth[length=2mm]}, thick},
  lab/.style={font=\footnotesize\itshape, text=black!60}
]
\node[data] (q) {Question};
\node[lm, right=7mm of q] (read) {Reader and planner\\\footnotesize constrained program};
\node[det, right=7mm of read] (check) {Type check\\and grounding};
\node[det, right=7mm of check] (exec) {Deterministic\\execution};
\node[data, right=7mm of exec] (out) {Answer or\\abstention};
\draw[arr] (q) -- (read);
\draw[arr] (read) -- (check);
\draw[arr] (check) -- (exec);
\draw[arr] (exec) -- (out);

\node[data, below=10mm of read] (cand) {Sampled candidate\\programs};
\node[det, right=8mm of cand] (match) {Execute and compare\\with benchmark oracle};
\node[data, right=8mm of match] (pool) {Accepted\\training traces};
\draw[arr] (cand) -- (match);
\draw[arr] (match) -- (pool);
\draw[arr, dashed] (pool.north) |- (read.south) node[pos=0.2, right, lab] {train};
\node[lab, below=2mm of match] {training-data construction only};
\end{tikzpicture}
\caption{System overview. The deployment path ends with deterministic
execution. Oracle comparison is used only when constructing training data.}
\label{fig:architecture}
\end{figure}

\subsection{Data conventions and canonical representation}
\label{sec:data}

The source corpus contains 166{,}277 compiled OCDS releases from 2022 to 2026.
A field audit identified two issues that determine many downstream answers.
First, signed values are often stored in contract sub-records rather than the
nominal award-value field. Second, most buyer and supplier references are
platform account identifiers. One real organisation can therefore appear
under many IDs. The Ministry of Defence appears under 77 platform identifiers
in one year of the source data.

We convert the releases into a canonical representation with 215{,}221 award
records and 131{,}502 organisations. Entity resolution is precision-first.
Registry identifiers and safe deterministic variants are merged before
normalised name and regional evidence are considered. Fuzzy matching produces
review candidates but never writes a merge directly. Monetary values follow a
fixed fallback rule and retain their source field and an additivity flag. The
flag prevents repeated framework ceilings from being summed once for every
linked award. The representation is materialised as typed columnar tables.
This supports vectorised joins, unit tests, and a second evaluator that does not
share execution code with the main system.

\subsection{Program language and deterministic execution}
\label{sec:algebra}

Questions are represented as trees in a closed typed algebra. Seven value
types and seventeen procurement operators cover filtering, projection,
counting, guarded summation, grouping, ranking, scalar comparison, and set
operations. Each node has an explicit input and output type. The complete
inventory appears in Appendix~\ref{app:algebra}. One sealed PACS question
illustrates the full path from language to answer.

\begin{quote}\small
\textit{Compare two slices, works notices during 2025 on one side, services
notices during 2022 on the other. Does the first side hold more notices?}

\smallskip
\texttt{combine[gt](count(filter(category=works, year=2025)),}\\
\texttt{\phantom{combine[gt](}count(filter(category=services, year=2022)))}

\smallskip
The program type-checks to \textsc{bool}, both branches execute over the
full record universe, and the executed answer is \texttt{False}, which
matches the oracle. The answer arrives with the record sets of both
branches, so a reviewer disputes the program, not a paragraph.
\end{quote}

The algebra is narrower than SQL or Python, but it provides three useful
properties. Membership is decidable by a recursive type checker. The grammar
can be compiled into a JSON schema for token-level constrained decoding
\citep{kwon2023vllm}. The operator set is small enough for a second evaluator
to implement independently. After a program is produced, the grounding layer
resolves entities, fields, dates, and values to the canonical representation.
The executor applies contract-level deduplication, exact decimal arithmetic,
the additivity guard, empty-key exclusion, and deterministic tie breaking. It
returns both the answer and the records used to compute it.

\subsection{Planning, training, and abstention}
\label{sec:planning}

The procurement system uses two learned stages. A reader extracts the requested
answer type, question literals, and dependency structure. A planner compiles
the question and brief into a program. This split was retained because it
outperformed direct generation on procurement. The margin is quantified by a
cross-base comparison. The fully local system on the weaker base outperforms
the hybrid system on the stronger base by 5.30 points with a 95\% interval of
3.62 to 6.97, which indicates that trained question reading contributes more
than base capability on this task. The WikiTableQuestions transfer
uses a fused reader-planner because fixed handoffs were weaker during table
development. All local systems, including untrained baselines, use the same
constrained decoder. This prevents output-format errors from dominating the
comparison.

Training candidates are sampled from a cloud teacher or from the local student.
A candidate is retained when it has a valid type structure, grounds to the
question and data, executes successfully, and produces the oracle answer. The
oracle does not author the candidate and is not included in the model input.
The training ladder includes supervised fine-tuning, self-harvest with
rejection sampling, and preference optimisation. Preference optimisation is
not used in the primary systems because its near-miss negatives were unstable
across the two bases.

Abstention is part of the output space. The system can return ambiguous,
unsupported, or no-result when the question or evidence does not justify an
answer. A repair step is available only after a typed failure. Refusals and
meaningful empty results are final, since an earlier ungated repair design
changed six correct refusals into incorrect answers.

\subsection{Benchmark construction and oracle audit}
\label{sec:benchmark-build}

The main benchmark is generated program first. Parameterised programs are
instantiated and executed before a question surface is written, so every row is
created with an executable gold program and answer. The benchmark contains
12{,}828 questions, including answerable, ambiguous, unsupported, and
empty-result cases, split into 9{,}267 training rows, three development
slices, and a sealed test of 2{,}285 rows. Splits separate program
structures rather than surface wording. Mechanical gates prevent plan overlap, duplicate identifiers, local
sampling omissions, and multi-answer ambiguity.

A second evaluator reconstructs answers from the raw releases without shared
execution code. Across 14{,}770 audited oracle cases, the two implementations
agree in 99.88\% of cases. All 18 remaining disagreements were inspected, and
none affects a reported comparison. This audit measures implementation
agreement under the declared data conventions. It does not remove uncertainty
from the source records themselves.

% -----------------------------------------------------------------------------
\section{Evaluation}
\label{sec:evaluation}

\paragraph{Benchmarks.}
The main evaluation uses a sealed test of 2{,}285 procurement questions. PACS
contains 922 rows from seven procurement-analysis families and tests whether
the system depends on the surface generator used for the main benchmark. Its
specification, question grammar, and logical-shape labels were frozen before
the model was evaluated. Five isolation checks cover text, program hash,
template identity, entity anchors, and logical shape. Each sealed intent is
rendered through an independently written surface grammar, and naturalised
rewrites pass deterministic checks of operation, negation, comparison
direction, quantifier, and scope against the gold program's logical
signature. WikiTableQuestions
contains 22{,}033 crowd-written questions over 2{,}108 web tables
\citep{pasupat2015compositional}. Development uses table-disjoint folds, and
the official 4{,}344-row test is evaluated once under a recorded manifest.

\paragraph{Systems.}
Table~\ref{tab:systems} shows the five main procurement systems. The local
models are Qwen3-8B and Llama-3.1-8B, trained with rank-64 LoRA under 4-bit
QLoRA \citep{hu2022lora,dettmers2023qlora}. All local systems use the same
executor and constrained decoder. The cloud teacher uses its provider API and
cannot use the same token-level grammar constraint, so teacher comparisons are
reported as deployable-system comparisons rather than equal-decoding model
comparisons.

\begin{table}[t]
\centering
\caption{Main procurement systems. All systems use the same data layer and
executor.}
\label{tab:systems}
\small
\begin{tabular}{llll}
\toprule
System & Question reading & Program planning & Training \\
\midrule
Cloud teacher & Cloud & Cloud & Zero-shot \\
Llama hybrid & Cloud & Llama-3.1-8B & In-domain \\
Qwen hybrid & Cloud & Qwen3-8B & In-domain \\
Llama fully local & Llama-3.1-8B & Llama-3.1-8B & In-domain \\
Qwen fully local & Qwen3-8B & Qwen3-8B & In-domain \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Protocol.}
Each primary evaluation uses one temperature-zero model call and no repair.
Answerable questions use type-aware exact match against the audited oracle.
Refusal questions use strict status match and a faithfulness-gated metric that
checks whether every literal in the emitted program is supported by the
question. Paired systems are compared with exact McNemar tests
\citep{mcnemar1947note}. PACS intervals use cluster bootstrap over intent
clusters. The cloud teacher cannot be seeded, so its variability was measured
with three identical development runs at $72.6\pm1.1$ points, and no teacher
comparison below three points rests on a single run. Each student is a
single frozen training run. Paired significance therefore measures test-set
disagreement and not training-seed variance. Full training configurations,
serving settings, compute footprint and the audit rule behind every
reported rate appear in Appendix~\ref{app:config}.

% -----------------------------------------------------------------------------
\section{Results}
\label{sec:results}

\subsection{Where the procurement performance comes from}
\label{sec:home}

This subsection makes two moves, and their data roles differ by design.
The decomposition of where performance comes from runs on development data,
because intermediate training arms are never sent to the sealed test, which
admits one pass per frozen configuration. The sealed test then confirms the
frozen champion systems. Figure~\ref{fig:home-ladder} shows both moves side
by side.

On development, the retrieval baseline reaches 31.5\%. An untrained
Qwen3-8B routed through the typed program interface and executor reaches
70.4\%, already close to the cloud teacher's replicate band, and the
untrained Llama-3.1-8B reaches 60.0\% through the same interface.
Supervised fine-tuning raises the two bases to 81.2\% and 83.1\%.
Rejection-sampling fine-tuning on the student's own verified survivors,
with the teacher absent, holds level at 81.5\% and 82.7\%, which shows the
verified pool rather than continued teacher access carries the signal.
Preference optimisation then splits by base, 83.5\% on Qwen against 77.3\%
on Llama, the instability that removed it from the primary systems, and
training both the reading and planning stages reaches 86.2\% and 84.2\%.
The large first step shows that exhaustive computation and a fixed
execution contract provide much of the initial gain. The three training
arms branch from the supervised model rather than forming a sequence, and
the fully local rung changes a different axis, replacing the cloud reader
with a trained local one on top of the best arm per base.

\begin{figure}[t]
\centering
\includegraphics[width=0.88\linewidth]{gain_ladder.pdf}
\caption{Home-benchmark results. Colours denote the base model in both
panels, and in panel (b) hatching marks the systems that read questions
with the cloud model; in panel (a) every rung below the top one uses the
cloud reader. (a) Development decomposition, from the model-independent retrieval
baseline through the untrained scaffolded base and the three training
arms that branch from the supervised model, to the top rung that replaces
the cloud reader with a trained local one on top of the best arm per
base. The preference-tuned arm shows
the base-dependent instability discussed in
Section~\ref{sec:planning}, and the shaded band covers three cloud-teacher
runs. (b) Sealed-test confirmation of the five frozen systems. The
retrieval baseline and the untrained base were not part of the frozen
sealed run on this benchmark, and their sealed measurements appear on the
challenge set in Table~\ref{tab:pacs-family}. Intermediate arms never
touch the sealed test, which admits one pass per frozen configuration.}
\label{fig:home-ladder}
\end{figure}

A format diagnostic supports reading the ladder as planning quality rather
than output discipline. Decoded without constraints on 100 development
questions, the untrained base produces question-grounded plans with real
entities in an invented format on 98 and matches the executable contract on
3, while the tuned models match the contract on 98 and 99. Fine-tuning
therefore teaches conformance to the specific contract, and constrained
decoding grants that contract to every system, so accuracy differences under
the shared decoder measure planning.

In the sealed confirmation of Figure~\ref{fig:home-ladder}(b), the
strongest local system is
15.89 points above the teacher, with a 95\% paired interval of 14.07 to 17.70
and exact McNemar $p<10^{-14}$. The same ordering holds with the second base,
which reduces the chance that the result is specific to one model family.
Fully local systems also outperform the hybrids on both bases, by 7.61
points with interval 6.10 to 9.13 on the stronger base. This indicates
that task-specific question reading contributes beyond local program planning.

Figure~\ref{fig:heatmap} resolves the sealed result by question type,
which turns the aggregate ordering into a mechanism. The teacher's
weakness concentrates exactly where exhaustive computation lives, 48.7\%
on counts and 61.3\% on sums against 76.2\% and 92.6\% for the strongest
local system, which is the failure retrieval-style reasoning cannot
escape. Comparison questions expose a base-dependent skill, with the two
Qwen systems at 82.1\% and 85.8\% against 40.6\% and 44.3\% for the Llama
systems. And the refusal columns close the abstention claim on the main
benchmark, where the strongest local system recognises ambiguous,
empty-result and unsupported questions at 99.2\%, 100.0\% and 100.0\%.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{finaltest_heatmap.pdf}
\caption{Sealed final-test accuracy by system and question type, twelve
answerable types and three refusal classes. The teacher collapses on
exhaustive aggregation, comparison skill follows the base model, and the
fully local systems close the refusal classes near ceiling.}
\label{fig:heatmap}
\end{figure}

\subsection{Distribution shift and independent procurement questions}
\label{sec:robustness}

Comparing the two panels of Figure~\ref{fig:home-ladder} system by
system gives the distribution-shift result directly. The hybrid systems
lose 5.5 and 7.1 points from development to test, while the fully local
systems lose 0.5 and 0.9, and the pattern holds on both bases. Independent
evidence on genuinely hard questions comes from the sealed challenge set
below rather than from any development slice. The decay asymmetry is consistent with the fully local system depending
less on the cloud reader and the development surface distribution. It does
not by itself establish a causal robustness mechanism.

PACS tests new question surfaces and logical exposure. The frozen local planner
reaches 78.31\% strict accuracy on 922 sealed rows and 80.00\% on the
answerable subset. On the same sealed rows the untrained base scores
36.01\%, a retrieval baseline scores 21.90\%, and the cloud teacher scores
50.33\%, all under the same strict overall metric on the full 922 rows, so
every rung of the deployment ladder is present on questions none of the
systems authored. Table~\ref{tab:pacs-family} reports the finer per-family
numbers on the aligned value-oracle subset, whose denominators differ from
the strict overall by construction. The teacher API lacks the
local constrained decoder, and 23.1\% of its rows fail on format, so the margin
is not presented as a decoding-neutral model comparison.

Table~\ref{tab:pacs-family} breaks the sealed result into task families
and status classes, computed for all systems on identical row sets. The
local planner leads six of the seven families, most extremely on overlap
and exclusion, 97.1\% against 21.4\% for the teacher, whose free-form
programs rarely survive the set-operation semantics. The single exception
is disclosure and compliance, the weakest family for every system, where
the teacher edges the planner 40.8\% to 38.8\%. Universal quantification
over every notice of a buyer defeats all systems, and the closed algebra
does not rescue it. On the refusal axis the planner recognises every one
of the 138 unsupported sealed questions, against 91.3\% for the teacher
and 29.7\% for the untrained base, so scored abstention is not only a
design feature but a measured sealed-test behaviour.

\begin{table}[t]
\centering
\caption{Sealed PACS results by task family and status class, computed
for all systems on identical row sets. Family rows use the 655 answerable
rows with concrete value oracles. The refusal row uses the 138
unsupported-question golds. The empty-result class was affected by the
documented 46-row labelling defect, and only the planner was re-scored
under the corrected labels, marked $\dagger$. The retrieval baseline was
not run on status rows. Bold marks the best system per row.}
\label{tab:pacs-family}
\small
\begin{tabular}{lccccc}
\toprule
 & n & Local planner & Cloud teacher & Untrained base & Retrieval \\
\midrule
F1 spending and volume & 104 & \textbf{99.0} & 69.2 & 59.6 & 12.5 \\
F2 temporal change & 107 & \textbf{96.3} & 81.3 & 60.7 & 53.3 \\
F3 supplier concentration & 97 & \textbf{47.4} & 38.1 & 26.8 & 11.3 \\
F4 cross-buyer comparison & 107 & \textbf{93.5} & 43.0 & 54.2 & 34.6 \\
F5 overlap and exclusion & 103 & \textbf{97.1} & 21.4 & 20.4 & 3.9 \\
F6 relational composition & 88 & \textbf{88.6} & 31.8 & 36.4 & 18.2 \\
F7 disclosure and compliance & 49 & 38.8 & \textbf{40.8} & 34.7 & 4.1 \\
\midrule
All answerable, value oracles & 655 & \textbf{83.8} & 47.6 & 42.9 & 21.4 \\
Refusal, exact status & 138 & \textbf{100.0} & 91.3 & 29.7 & -- \\
Empty result, corrected labels & 90 & 38.9$^\dagger$ & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

The PACS audits, shown in Figure~\ref{fig:pacs-audits} of the appendix, separate depth from novelty.
A row is irreducible when deleting any predicate from its gold program changes
the answer. This holds for 42.7\% of answerable rows, and the planner scores
78.7\% on that subset, close to the 80.0\% answerable result. Intent shapes not
shown during training reduce accuracy to 68.5\%, a cost of 11.5 points with
a 95\% cluster bootstrap interval of 5.6 to 17.2. The system therefore transfers
new compositions of familiar constructs more reliably than entirely new
logical forms. The per-family breakdown in Appendix~\ref{app:audits}
locates the weakness precisely. Disclosure and compliance questions, which
require universal quantification over every notice of a buyer, score 38.8\%,
and the deep cells of supplier concentration, which bind a scope inside each
group, fall to between 15\% and 30\%, while the five remaining families
score between 82\% and 97\% on value-oracle rows. The repair result in the same figure is a development variant
and is not part of the sealed headline.

\subsection{Transfer to WikiTableQuestions}
\label{sec:wtq}

The transfer retains the typed execution architecture and replaces the
procurement data adapter with table views, a column-aware value linker, and one
row-preserving arg-extremum operator. Table~\ref{tab:wtq-results} reports the
official test results. The procurement checkpoint carries a 4.82-point
zero-shot gain over the untrained base. Answer-only training reaches 44.43\%,
and gold-program training reaches 51.80\%. The answer-only result is comparable
with classical weakly supervised parsers, but both trained systems remain below
recent specialised table-QA methods \citep{cheng2023binder,ye2023dater,
wang2024chainoftable}. The experiment is used to study portability and
bottlenecks, not to claim state of the art.

\begin{table}[t]
\centering
\caption{WikiTableQuestions results. Official sealed test (n=4{,}344,
one pass under a recorded manifest), each rung significant over the
previous at exact McNemar $p<5\times10^{-18}$, alongside the development
fold before and after representation and compiler closure.}
\label{tab:wtq-results}
\small
\begin{tabular}{lcccc}
\toprule
 & Official test & Step gain & Dev fold & after closure \\
\midrule
Untrained local base & 22.51 & & & \\
Procurement checkpoint, zero-shot & 27.33 & $+4.82$ & & \\
Answer-only training & 44.43 & $+17.10$ & 31.7 & 40.0 \\
Gold-program training & \textbf{51.80} & $+7.37$ & 40.0 & 51.0 \\
\bottomrule
\end{tabular}
\end{table}

A differential audit against 11{,}276 SQUALL SQL programs
\citep{shi2020squall} locates the remaining gap. The original algebra expresses
53.9\% of the reference programs. Compiler closure and typed loader views
recover 730 additional programs and raise coverage to 61.0\%. Of these, 562
come from translator closure, 154 from loader views, and 14 require both. The
remaining 39.6\% lie outside the language. Figure~\ref{fig:coverage-attribution} in the appendix
shows the resulting attribution. Translated in-coverage programs
match the reference denotation in 92--93\% of cases, and execution recovers the
benchmark answer in 94.1\% of those cases. The reference programs are
themselves imperfect, reproducing the benchmark answer in 88.9\% of cases, so
the audit treats them as a noisy reference rather than an absolute standard.
This pattern points to coverage rather than executor correctness as the main
transfer limit.

The coverage account is also tested by intervention. After closure and retraining, the
answer-only development model rises from 31.7\% to 40.0\%, and the gold-program
model rises from 40.0\% to 51.0\%, so both supervision regimes improve under
the same representation and compiler intervention. This is the clearest
evidence that the transfer gap is not solely a planning-model problem.

% -----------------------------------------------------------------------------
\section{Discussion and Limitations}
\label{sec:discussion}

The results support three conclusions. First, a deterministic execution
contract changes the problem substantially. An untrained local model becomes
competitive with the cloud teacher once both operate through a typed program
interface and executor. Second, task-specific training still matters. The two
fully local systems outperform their hybrid counterparts and degrade less from
development to test. Third, portability is bounded by what the data adapter,
compiler, and algebra can represent. The WikiTableQuestions audit measures this
boundary directly and shows that widening coverage improves both supervision
regimes.

The scale of the evidence supports deployment-level claims rather than
prototype-level ones. The system operates over the full five-year national
corpus of 215{,}221 award records and 131{,}502 organisations, answers a
12{,}828-question benchmark whose oracles are verified at 99.88\% by an
independent implementation, and was evaluated once on each of two sealed
test sets. The practical form is a local analyst for public procurement
that runs on a single GPU, answers exhaustive aggregation questions that
retrieval cannot, refuses when the data cannot support an answer, and
returns programs a reviewer can execute rather than prose a reviewer must
trust.

The teacher comparison is task-specific. The student receives in-domain
training, while the teacher is evaluated as a zero-shot deployment. The result
shows that the full local system is more accurate on the defined task, not that
an 8B model has greater general capability. PACS strengthens the external
validity claim, but it is still a controlled benchmark rather than a sample of
real analyst questions.

Five limitations remain. The main benchmark is program-first and its question
surfaces are generated under style controls. Each student result comes from one
training run, so training-seed variance is not estimated. The teacher and local
systems do not share identical decoding constraints. The closed algebra rejects
questions outside its supported operations, and unseen intent shapes remain a
clear weakness. The transfer study also lacks a
baseline in which a language model writes SQL that is executed directly,
and since the official test is consumed, that comparison belongs to future
work on development folds, with the differential audit as a partial answer,
since even the gold SQL annotations reproduce the benchmark answer in only
88.9\% of cases. Finally, the study does not simulate arbitrary deletion of KG
facts. Its refusal tests cover missing or unsupported evidence, but not every
form of data incompleteness.

Refusal did not transfer free of charge. On a paired comparison against
the earlier production system, the compositional planner wins the
answerable subset by 4.4 points yet loses explicit abstention clearly,
because its training mix carried only 37 refusal demonstrations. Under the
pre-registered bar of no key-bucket regression, no retirement claim is made
for the older system. Competence follows demonstrations, and refusal is a
competence like any other.

The candidate-selection experiment in Appendix~\ref{app:selection} provides an
additional negative result. An LLM selector does not improve over the first
accepted candidate, while a small deterministic scorer based on execution
features gains 2.0 points. This finding concerns best-of-$k$ selection only. It
is not evidence that all model-based verification is ineffective.

% -----------------------------------------------------------------------------
\section{Conclusion}
\label{sec:conclusion}

We presented an auditable question-answering framework for UK public
procurement. The framework combines explicit data conventions, typed program
generation, deterministic execution, abstention, and independent oracle audit.
The experiments show that the executable scaffold establishes a strong base,
local training of both reading and planning improves generalisation, and the
result transfers to independent procurement questions. The table-domain study
shows that representation and program-language coverage are the main remaining
limits.

\bibliography{references}
\bibliographystyle{tmlr}

\appendix

% -----------------------------------------------------------------------------
\section{Operator Algebra}
\label{app:algebra}

Programs are trees over seven value types. \textsc{records} is a subset of the
record universe, \textsc{values} is a distinct set of scalar values,
\textsc{groups} maps group keys to numbers, and \textsc{ranking} is an ordered
list of key-value pairs. \textsc{number}, \textsc{value}, and \textsc{bool}
are scalar types. Program depth is capped at 16 and node count at 64. Each
checker failure contains a typed diagnostic and a path into the program tree.

\begin{table}[h]
\centering
\caption{Closed operator inventory. The WikiTableQuestions transfer adds the
row-preserving \texttt{extreme\_rows} node.}
\label{tab:algebra}
\small
\begin{tabular}{lll}
\toprule
Node & Signature & Function \\
\midrule
\texttt{filter} & predicates $\to$ \textsc{records} & filter records \\
\texttt{values} & \textsc{records} $\to$ \textsc{values} & project distinct values \\
\texttt{count} & \textsc{records} $\to$ \textsc{number} & count records \\
\texttt{size} & \textsc{values} $\to$ \textsc{number} & set cardinality \\
\texttt{sum} & \textsc{records} $\to$ \textsc{number} & guarded money sum \\
\texttt{exists} & \textsc{records} $\to$ \textsc{bool} & test non-emptiness \\
\texttt{select} & \textsc{records} $\to$ \textsc{value} & unique field lookup \\
\texttt{extreme} & \textsc{records} $\to$ \textsc{value} & record argmin or argmax \\
\texttt{groupby} & \textsc{records} $\to$ \textsc{groups} & grouped count or sum \\
\texttt{argext} & \textsc{groups} $\to$ \textsc{value} & extremum group key \\
\texttt{top} & \textsc{groups} $\to$ \textsc{ranking} & top-$k$ groups \\
\texttt{num} & literal $\to$ \textsc{number} & numeric literal \\
\texttt{combine} & \textsc{number}$^2$ $\to$ scalar & arithmetic or comparison \\
\texttt{vcompare} & \textsc{value} $\to$ \textsc{bool} & scalar comparison \\
\texttt{setop} & \textsc{values}$^2$ $\to$ \textsc{values} & set operation \\
\texttt{gcombine} & \textsc{groups}$^2$ $\to$ \textsc{groups} & combine grouped values \\
\texttt{keys\_where} & \textsc{groups} $\to$ \textsc{values} & thresholded group keys \\
\texttt{extreme\_rows} & \textsc{records} $\to$ \textsc{records} & row-preserving arg-extremum \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------------
\section{Audit and Supplementary Results}
\label{app:audits}

\paragraph{Entity resolution.}
The source contains 204{,}711 raw organisation aliases. Resolution proceeds
through registry identifiers, safe deterministic variants, normalised name and
region, and finally name-only evidence. A total of 255 borderline cases were
placed in a review file, and 82{,}013 singletons were left unmerged. Similarity
matching never writes a merge directly.

\paragraph{Benchmark gates.}
Every generated program is re-executed against the full graph before its
question is admitted. The build rejects local sampling omissions, multi-answer
ambiguity, plan overlap, duplicate identifiers, and row-count changes. A
position-aware mechanical detector checks whether paraphrases preserve the
logical signature. Nineteen of the 2{,}285 final-test rows contain phrases also
seen in refusal traps. Removing them changes every headline by at most 0.21
points and changes no ranking.

\paragraph{PACS isolation.}
Five zero-overlap checks hold between training and sealed evaluation at the
level of question text, logical signature, template identity, entity anchors,
and program hash. A 93-row human audit took place before unsealing. It found one
systematic boolean error affecting 46 rows, which was corrected by offline
re-execution before any model score was read.

\begin{figure}[h]
\centering
\includegraphics[width=0.88\linewidth]{pacs_audits.pdf}
\caption{PACS audits. The irreducible subset remains close to the answerable
headline, while unseen intent shapes produce the largest measured loss. The
repair arm is a development-only variant.}
\label{fig:pacs-audits}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{attribution.pdf}
\caption{Attribution of WikiTableQuestions program coverage. Compiler and
loader closure raise measured coverage from 53.9\% to 61.0\%, while 39.6\%
remains outside the language.}
\label{fig:coverage-attribution}
\end{figure}

\paragraph{PACS status axis.}
Per-family and status-class results appear in
Table~\ref{tab:pacs-family} of the main text. On the 267 rows whose
serialised oracle is empty
the planner returns the exact gold status on 138 under raw scoring, and the
documented offline correction of the empty-result class credits a further
35 rows, which reconciles exactly with the reported overall of 722 correct
rows out of 922. Abstention-type golds alone are recognised at 138 of 194
rows, or 71.1\%.

\paragraph{Training ladder.}
Table~\ref{tab:ladder} reports development accuracy and data-engine acceptance
rates. Acceptance rate is the fraction of sampled answerable candidates that
pass the data pipeline, over the 8{,}555 answerable training questions.
The teacher pass accepts 5{,}605 traces, and the first self-harvest rounds
accept 6{,}556 and 6{,}685 on the two bases. It is not system accuracy. Acceptance rises across
self-harvest rounds while the evaluation gain of the second round is 0.4
points and not significant, so the loop is reported at its measured ceiling
rather than iterated further.

\begin{table}[h]
\centering
\caption{Development ladder and candidate acceptance rates.}
\label{tab:ladder}
\small
\begin{tabular}{lcc}
\toprule
Arm & Qwen3-8B & Llama-3.1-8B \\
\midrule
Zero-shot scaffolded & 70.4 & 60.0 \\
Supervised fine-tuning & 81.2 & 83.1 \\
Rejection-sampling fine-tuning & 81.5 & 82.7 \\
Preference optimisation & 83.5 & 77.3 \\
Fully local, best arm & 86.2 & 84.2 \\
\midrule
Teacher candidate acceptance & \multicolumn{2}{c}{65.5} \\
Self-harvest acceptance, round 1 & 76.6 & 78.1 \\
Self-harvest acceptance, round 2 & 86.3 & -- \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------------
\section{Candidate Selection}
\label{app:selection}

Four candidate programs are sampled per development question after the basic
program checks. A perfect selector would gain about five points over choosing
the first accepted candidate. The deterministic scorer uses only execution
features such as empty results, zero-row filter probes, and answer-kind
mismatches. It gains 2.0 points (Figure~\ref{fig:candidate-selection}). A
faithful LLM selector gains 0.0, and an immediate LLM score loses 0.7. This
experiment evaluates candidate ranking and not the main single-call system.

\begin{figure}[h]
\centering
\includegraphics[width=0.82\linewidth]{judges.pdf}
\caption{Candidate selection on development data. The deterministic scorer is
the only selector that improves over the first accepted candidate. Crosses
show the oracle best of four.}
\label{fig:candidate-selection}
\end{figure}

% -----------------------------------------------------------------------------
\section{Reproduction Details}
\label{app:config}

Students are Qwen3-8B \citep{qwen3-2025} and Llama-3.1-8B. Both are adapted
with rank-64 LoRA under 4-bit QLoRA quantisation in LLaMA-Factory
\citep{hu2022lora,dettmers2023qlora,zheng2024llamafactory}. Serving uses vLLM
with guided JSON decoding compiled from the algebra grammar
\citep{kwon2023vllm}. Candidate collection uses positive-temperature sampling,
while evaluation uses one temperature-zero call. Training and evaluation ran
on single 80GB A100 and 96GB H100 devices. Each fine-tuning run completes
within a few hours on one device, every sealed evaluation is a single pass,
the teacher harvest comprises one pass over the 9{,}267-question training
pool, and the final-test teacher replicate adds roughly 3{,}000 further
teacher calls. The WikiTableQuestions official run
used a recorded manifest containing the commit hash, file checksums, decoder
settings, and adapter identity. An experiment ledger records every run behind
this paper, and any rate-style number enters the ledger only after ten
deterministically sampled raw transcripts, passes and failures both, have
been read by a human. Prompts, manifests, tests, and the experiment
ledger are released with the code.

\end{document}
